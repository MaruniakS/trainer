$("<%= "#myModal_exercise#{@exercise.id}" %>").hide();
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();
$("<%= "#ex_#{@exercise.id}" %>").html("<%= escape_javascript(render partial: 'programs/training_day_exercise', locals: { day_exercise: @exercise} ) %>");